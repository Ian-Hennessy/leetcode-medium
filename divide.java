public class divide
{
    public static int main(int divisor, int dividend)
    {
        boolean is_negative = false;
        if (dividend < 0 | divisor < 0)
        {
            boolean is_negative = true;
            divisor = Math.abs(divisor);
            dividend = Math.abs(dividend);
        }

        int


    }
}